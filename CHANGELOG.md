# Changelog

## 1.1.0

Theme-native bar colors and radio controls.

- On air uses the theme accent color; tuning and paused use muted; urgent red is only for errors
- Panel: Pause, Skip, Back, From the start, Retry, and Join live when Marc is streaming
- Elapsed time in the current session (“1h 12m into a 3h session”)
- Skip silently over gone/blocked videos; surface an error after a few failures, with Retry
- Login no longer auto-plays; a shell restart on the same boot still resumes
- Media-key play/pause still works through MPRIS

## 1.0.1

Marketplace security hardening: closed PATH, bounded catalog refresh, private state directory.

## 1.0.0

First listing: random Marc Rebillet sessions, audio only, mid-stream tune-in.
